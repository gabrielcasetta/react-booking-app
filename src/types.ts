export interface Booking {
    id: string;
    name: string;
    property: string;
    startDate: string;
    endDate: string;
  }
  
export interface Property {
  id: string;
  name: string;
  description: string;
  image: any;
}